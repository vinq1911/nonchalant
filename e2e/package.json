{
  "name": "nonchalant-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end browser tests for nonchalant media server (WebSocket-FLV playback, HTTP-FLV, API).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
